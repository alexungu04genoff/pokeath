# Ulaula full-board sloped-lap integration experiment

`output/CURRENT/prototype/ulaula_sloped_full_board/` is **EXPERIMENTAL / NOT PRODUCTION / PHYSICAL TEST PENDING**. It is a four-quadrant Ulaula rebuild with prototype-derived sloped-lap pin joints at the three production seam endpoints. It never replaces files under `output/CURRENT/boards/`.

Run `python -m src.prototypes.build_ulaula_sloped_full_board` to regenerate the experimental STLs, reference scene, previews, and validation report. The generator snapshots and rechecks all 16 canonical production hashes.
